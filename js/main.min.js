function app() {
    const t = document.querySelectorAll(".buttonfilter"),
        e = document.querySelectorAll(".tickets");
    t.forEach(t => {
        t.addEventListener("click", () => {
            const s = t.dataset.filter;
            var l;
            l = s, e.forEach(t => {
                const e = !t.classList.contains(l);
                isShowAll = "all" === l.toLowerCase(), e && !isShowAll ? t.classList.add("hide") : t.classList.remove("hide")
            })
        })
    })
}
//# sourceMappingURL=../sourcemaps/main.min.js.map